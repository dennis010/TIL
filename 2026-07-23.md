# 2026-07-23 (Day 2)

## 오늘 한 것
- Homebrew 설치 (6.0.12), VS Code brew로 재설치
- 터미널: mkdir로 dev 폴더 생성, cd로 이동
- 점프 투 파이썬 02-1 숫자형, 02-2 문자열
- 파일명 분해 실습 (split → 인덱싱 → f-string)

## 새로 알게 된 것
- Error(빨강) = 멈춤 / Warning(노랑) = 알려주고 계속 진행
- \n = 줄바꿈 문자. 들여쓰기(Tab)와는 다른 것
- 변수만 치면 원본 그대로 보여주고, print()를 써야 사람용 출력(\n이 진짜 줄바꿈됨)
- split("_") = 기준 문자로 쪼개서 리스트로 만들어줌
- 슬라이싱 [:8] = 0은 생략 가능
- 리스트도 parts[0]처럼 인덱싱 가능

## 용어: PoC
PoC(개념증명) = 본격 투자 전에, 아이디어가 실제로 되는지 작은 규모로 검증하는 단계.

## 막혔던 것 / 헷갈리는 것
- VS Code 설치 시 "이미 있음" Error → --force로 해결
- \n이 왜 안 먹히는지 몰랐음 → print() 안 써서였음
- 문자열 함수 나열 구간에서 집중력 잃음 → 외우지 말고 검색하면 된다고 함
- 문자열 아직 80% 이해 (구멍은 쓰면서 메우기로)

## 내일 할 것
- 문자열 워밍업 드릴 5문제 + 리스트
